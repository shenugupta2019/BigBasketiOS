//
//  ContentView.swift
//  BigBasket
//
//  Created by Shenu Gupta on 29/10/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .foregroundColor(Color.yellow)
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!dcad")
                .font(.headline)
                .foregroundColor(Color.red)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
